lado = input("Informe a medida do lado da sala: ");
if (lado > 0) then
    area = lado * lado
    printf("A area da sala é %f", area);
else
    printf("O valor informado é inválido: %f", lado);
end
